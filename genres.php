<?php require_once __DIR__.'/bootstrap.php';$title='Genres — AniRead';include __DIR__.'/includes/header.php';?><h1>Genres</h1><div class="grid"><?php foreach($pdo->query('SELECT g.*,COUNT(ag.anime_id) total FROM genres g LEFT JOIN anime_genres ag ON ag.genre_id=g.id GROUP BY g.id ORDER BY g.name') as $g):?><a class="card" style="padding:25px" href="genre.php?id=<?=$g['id']?>"><h2><?=e($g['name'])?></h2><p class="muted"><?=$g['total']?> anime</p></a><?php endforeach;?></div><?php include __DIR__.'/includes/footer.php';
