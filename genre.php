<?php require_once __DIR__.'/bootstrap.php';$id=(int)($_GET['id']??0);$s=$pdo->prepare('SELECT * FROM genres WHERE id=?');$s->execute([$id]);$g=$s->fetch();if(!$g)exit('Genre not found');$title=$g['name'].' Anime — AniRead';include __DIR__.'/includes/header.php';?><h1><?=e($g['name'])?> anime</h1><div class="grid"><?php $s=$pdo->prepare('SELECT a.* FROM anime a JOIN anime_genres ag ON ag.anime_id=a.id WHERE ag.genre_id=? ORDER BY a.popularity');$s->execute([$id]);foreach($s as $a)card($a);?></div><?php include __DIR__.'/includes/footer.php';
