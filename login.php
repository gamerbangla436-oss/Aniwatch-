<?php require_once __DIR__.'/bootstrap.php';$error='';if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf();$s=$pdo->prepare('SELECT * FROM users WHERE email=? AND active=1');$s->execute([trim($_POST['email'])]);$u=$s->fetch();if($u&&password_verify($_POST['password'],$u['password'])){session_regenerate_id(true);$_SESSION['user_id']=$u['id'];header('Location: '.url());exit;}$error='Invalid email or password.';}$title='Login — AniRead';include __DIR__.'/includes/header.php';?><h1>Welcome back</h1><?php if($error):?><p class="muted"><?=e($error)?></p><?php endif;?><form class="form" method="post"><input type="hidden" name="csrf" value="<?=csrf()?>"><label>Email</label><input class="input" type="email" name="email" required><label>Password</label><input class="input" type="password" name="password" required><br><button class="btn">Login</button><p>New here? <a href="register.php">Create an account</a></p></form><?php include __DIR__.'/includes/footer.php';
