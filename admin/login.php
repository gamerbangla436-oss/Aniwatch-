<?php require_once __DIR__.'/../bootstrap.php';if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf();$s=$pdo->prepare('SELECT id FROM users WHERE email=? AND is_admin=1 AND active=1');$s->execute([trim($_POST['email']??'')]);$id=$s->fetchColumn();$q=$pdo->prepare('SELECT * FROM users WHERE id=?');$q->execute([$id]);$u=$q->fetch();if($u&&password_verify($_POST['password']??'',$u['password'])){session_regenerate_id(true);$_SESSION['user_id']=$u['id'];header('Location: '.url('admin/index.php'));exit;}$error='Invalid administrator credentials.';}$title='Admin login — AniRead';include __DIR__.'/../includes/header.php';?><h1>Administrator login</h1><p class="muted"><?=e($error??'')?></p><form class="form" method="post"><input type="hidden" name="csrf" value="<?=csrf()?>"><label>Email</label><input class="input" type="email" name="email" required><label>Password</label><input class="input" type="password" name="password" required><br><button class="btn">Sign in</button></form><?php include __DIR__.'/../includes/footer.php';
